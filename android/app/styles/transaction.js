import { StyleSheet } from 'react-native';


const styles = StyleSheet.create({
    transactionType: {
        alignSelf: 'center',
        flexDirection: 'row',
        alignItems: 'center',
    },
    user: {
        alignItems: 'center',
        padding: 8,
    },
    avatar: {
        fontSize: 48,
    },
    arrow: {
        fontSize: 32,
    },
});

export default styles;
